# Используем последний официальный образ Golang как базовый
FROM golang:latest

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем все файлы из текущей директории в рабочую директорию контейнера
COPY . .

# Компилируем Go-приложение, создавая исполняемый файл main
RUN go build -o main cmd/main.go

# Указываем Docker, что контейнер будет использовать порт 8081
EXPOSE 8081

# Определяем команду для выполнения при запуске контейнера
CMD ["./main"]
